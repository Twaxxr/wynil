<Window x:Class="Wynil.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:controls="clr-namespace:Wynil.App.Controls"
        xmlns:views="clr-namespace:Wynil.App.Views"
        Title="{Binding ProductName}" Icon="/Wynil.App;component/Assets/logo.png" Width="1450" Height="900" MinWidth="1100" MinHeight="700"
        WindowStartupLocation="CenterScreen" WindowStyle="None" ResizeMode="CanResize" Background="{DynamicResource AppBackgroundBrush}"
        Foreground="{DynamicResource PrimaryTextBrush}" TextOptions.TextFormattingMode="Display">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="7" CornerRadius="11" GlassFrameThickness="0" UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>
    <Window.Resources>
        <Style x:Key="SignalBar" TargetType="Rectangle">
            <Setter Property="Width" Value="2"/><Setter Property="Fill" Value="{DynamicResource SuccessBrush}"/><Setter Property="Margin" Value="1,0"/><Setter Property="VerticalAlignment" Value="Bottom"/>
            <Style.Triggers><DataTrigger Binding="{Binding SignalActive}" Value="False"><Setter Property="Opacity" Value="0.25"/></DataTrigger></Style.Triggers>
        </Style>
    </Window.Resources>

    <Border x:Name="WindowFrame" CornerRadius="11" BorderBrush="#405369" BorderThickness="1" Background="{DynamicResource AppBackgroundBrush}" ClipToBounds="True">
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="116"/><RowDefinition Height="*"/><RowDefinition Height="54"/></Grid.RowDefinitions>

            <Grid x:Name="TitleBar" Background="#35030A12" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="32,0,0,0">
                    <Border Width="62" Height="62" CornerRadius="11" BorderBrush="#805B8CE9" BorderThickness="1" Background="#0B111A" Margin="0,0,24,0">
                        <Image Source="/Wynil.App;component/Assets/logo.png" Width="52" Height="52" Stretch="Uniform" />
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="{Binding ProductName}" FontSize="31" FontWeight="Bold" />
                        <TextBlock Text="A cinematic desktop for whatever is playing." Style="{StaticResource MutedText}" Margin="0,6,0,0" />
                    </StackPanel>
                </StackPanel>

                <Grid Grid.Column="1">
                    <Grid.RowDefinitions><RowDefinition Height="34"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="—" Style="{StaticResource WindowButton}" Click="Minimize_Click" ToolTip="Minimize" AutomationProperties.Name="Minimize window" />
                        <Button x:Name="MaximizeButton" Content="□" Style="{StaticResource WindowButton}" Click="Maximize_Click" ToolTip="Maximize" AutomationProperties.Name="Maximize window" />
                        <Button Content="×" Style="{StaticResource WindowButton}" Click="Close_Click" ToolTip="Close to tray" AutomationProperties.Name="Close window" />
                    </StackPanel>
                    <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,30,0">
                        <Button Command="{Binding SaveSettingsCommand}" Style="{StaticResource BaseButton}" Margin="0,0,12,0" ToolTip="Save changed settings">
                            <StackPanel Orientation="Horizontal"><TextBlock Text="" FontFamily="Segoe Fluent Icons" FontSize="16" Margin="0,0,9,0"/><TextBlock Text="Save settings"/></StackPanel>
                        </Button>
                        <Button Command="{Binding ToggleWallpaperCommand}" Style="{StaticResource PrimaryButton}" Content="{Binding WallpaperButtonContent}" MinWidth="164" />
                    </StackPanel>
                </Grid>
            </Grid>

            <Grid Grid.Row="1" Margin="32,0,32,24">
                <Grid.ColumnDefinitions><ColumnDefinition Width="0.84*" MinWidth="410"/><ColumnDefinition Width="22"/><ColumnDefinition Width="1.16*" MinWidth="570"/></Grid.ColumnDefinitions>
                <controls:NowPlayingCard x:Name="NowPlayingCard" />

                <Border x:Name="SettingsHost" Grid.Column="2" Style="{StaticResource Card}" Padding="24,12,24,14">
                    <TabControl SelectedIndex="1">
                        <TabItem>
                            <TabItem.Header><StackPanel Orientation="Horizontal"><TextBlock Text="" FontFamily="Segoe Fluent Icons" FontSize="16" Margin="0,0,8,0"/><TextBlock Text="General"/></StackPanel></TabItem.Header>
                            <views:GeneralSettingsView />
                        </TabItem>
                        <TabItem>
                            <TabItem.Header><StackPanel Orientation="Horizontal"><TextBlock Text="" FontFamily="Segoe Fluent Icons" FontSize="16" Margin="0,0,8,0"/><TextBlock Text="Appearance"/></StackPanel></TabItem.Header>
                            <views:AppearanceSettingsView />
                        </TabItem>
                        <TabItem>
                            <TabItem.Header><StackPanel Orientation="Horizontal"><TextBlock Text="" FontFamily="Segoe Fluent Icons" FontSize="16" Margin="0,0,8,0"/><TextBlock Text="Animation"/></StackPanel></TabItem.Header>
                            <views:AnimationSettingsView />
                        </TabItem>
                        <TabItem>
                            <TabItem.Header><StackPanel Orientation="Horizontal"><TextBlock Text="" FontFamily="Segoe Fluent Icons" FontSize="16" Margin="0,0,8,0"/><TextBlock Text="Media"/></StackPanel></TabItem.Header>
                            <views:MediaSettingsView />
                        </TabItem>
                        <TabItem>
                            <TabItem.Header><StackPanel Orientation="Horizontal"><TextBlock Text="" FontFamily="Segoe Fluent Icons" FontSize="16" Margin="0,0,8,0"/><TextBlock Text="Performance"/></StackPanel></TabItem.Header>
                            <views:PerformanceSettingsView />
                        </TabItem>
                        <TabItem>
                            <TabItem.Header><StackPanel Orientation="Horizontal"><TextBlock Text="&#xE9D9;" FontFamily="Segoe Fluent Icons" FontSize="16" Margin="0,0,8,0"/><TextBlock Text="Diagnostics"/></StackPanel></TabItem.Header>
                            <views:DiagnosticsSettingsView />
                        </TabItem>
                    </TabControl>
                </Border>
            </Grid>

            <Border Grid.Row="2" Background="#30030A12" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0">
                <Grid Margin="32,0">
                    <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Ellipse Width="9" Height="9" Fill="{DynamicResource SuccessBrush}" Margin="0,0,12,0"><Ellipse.Effect><DropShadowEffect Color="#54D28A" BlurRadius="8" ShadowDepth="0" Opacity="0.45"/></Ellipse.Effect></Ellipse>
                        <TextBlock Text="{Binding Status}" Style="{StaticResource MutedText}" VerticalAlignment="Center" />
                        <StackPanel Orientation="Horizontal" Height="18" Margin="16,0,0,0" VerticalAlignment="Center">
                            <Rectangle Style="{StaticResource SignalBar}" Height="5"/><Rectangle Style="{StaticResource SignalBar}" Height="9"/><Rectangle Style="{StaticResource SignalBar}" Height="13"/><Rectangle Style="{StaticResource SignalBar}" Height="17"/>
                        </StackPanel>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Visibility="Collapsed">
                        <TextBlock Text="·" Foreground="#526176" VerticalAlignment="Center" Margin="5,0,0,0"/>
                        <TextBlock Text="·" Foreground="#526176" VerticalAlignment="Center" Margin="5,0,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.RowSpan="3" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,70" Background="{DynamicResource ElevatedBrush}" BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1" CornerRadius="7" Padding="18,11" Panel.ZIndex="20"
                    Visibility="{Binding IsToastVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel Orientation="Horizontal"><TextBlock Text="" FontFamily="Segoe Fluent Icons" Foreground="{DynamicResource SuccessBrush}" Margin="0,0,9,0"/><TextBlock Text="{Binding ToastText}" FontWeight="SemiBold"/></StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
