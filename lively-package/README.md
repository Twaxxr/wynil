# Lively Wallpaper package

Run `scripts/build.ps1`; it copies the production frontend into this directory. Run `configure-lively.ps1` once to add your per-install local authentication token, then choose **Add Wallpaper** in Lively and import this folder. Keep the Wynil companion running to supply metadata. The connection is bound to loopback and rejects missing or incorrect tokens.
